# 主控环境配置
# 用于运行Pipeline主程序和协调各预测器
name: master_env
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pyyaml>=6.0
  - pandas>=1.4.0
  - numpy>=1.21.0
  - biopython>=1.79
  - tqdm>=4.64.0
  - pip:
    - plotly>=5.8.0
    - jinja2>=3.0.0
    - pytest>=7.0.0
