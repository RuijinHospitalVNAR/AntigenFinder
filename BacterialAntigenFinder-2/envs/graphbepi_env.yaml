# GraphBepi 环境配置
# 使用CUDA 12.1兼容的PyTorch
name: graphbepi_env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24
  - pandas>=1.4
  - biopython>=1.79
  - tqdm>=4.64.0
  - pip:
    - fair-esm>=2.0.0
    - pytorch-lightning>=2.0
